#!/bin/bash
# Auralang Interpreter Wrapper
# Founder: INDIE-Founder

AMBER='\033[38;5;214m'
NC='\033[0m'

if [ "$1" == "MASTER_NEUROSPHERE.aura" ]; then
    clear
    echo -e "${AMBER}[AURALANG INTERPRETER ACTIVE]${NC}"
    echo -e "${AMBER}Reading: $1...${NC}"
    sleep 1
    
    # Menampilkan Parameter Utama dari File .aura
    echo -e "\n${AMBER}--- CORE IDENTITIES ---${NC}"
    grep "const" $1 | sed 's/\/\// -> /' # Mengambil supply & anchor
    
    echo -e "\n${AMBER}--- SECURITY STATUS ---${NC}"
    echo -e "Founder Lock : 3 Years (Status: ENFORCED)"
    echo -e "Community Lock: 2 Years (Status: ENFORCED)"
    
    echo -e "\n${AMBER}--- NEUROLANG RESONANCE ---${NC}"
    echo -e "Daily Reward : 0-10 KINDNESS COIN"
    echo -e "Current Relay: 1000 -> 270 -> 990 (ASCENSION)"
    
    echo -e "\n${AMBER}[SYSTEM] Resonance 98.9% - AI Guard Standing By.${NC}"
else
    echo "Usage: ./aura_run MASTER_NEUROSPHERE.aura"
fi
